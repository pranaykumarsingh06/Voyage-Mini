# Git Workflow Rule

- **Always Push**: Whenever code changes or new features are added, tested, or modified, commit the changes with a clear commit message and immediately push to `origin main` on GitHub (`https://github.com/pranaykumarsingh06/Voyage-Mini.git`).
- Ensure the working tree is clean and synced with remote after tasks.
